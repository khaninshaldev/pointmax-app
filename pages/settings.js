import React from "react";
import SignOutBtn from "../features/common/SignOutBtn";

const settings = () => {
  return (
    <section>
      <SignOutBtn />
    </section>
  );
};

export default settings;
